# Subtitle Translator - 项目模块分析文档

> 版本: 2.2.0
> 作者: rockbenben
> 技术栈: Next.js 16.1.6 + React 19 + TypeScript + Tailwind CSS 4

---

## 📋 目录

- [1. 项目概述](#1-项目概述)
- [2. 技术架构](#2-技术架构)
- [3. 目录结构](#3-目录结构)
- [4. 核心模块详解](#4-核心模块详解)
  - [4.1 翻译引擎模块 (`src/app/lib/translation/`)](#41-翻译引擎模块)
  - [4.2 React 组件模块 (`src/app/components/`)](#42-react-组件模块)
  - [4.3 自定义 Hooks 模块 (`src/app/hooks/`)](#43-自定义-hooks-模块)
  - [4.4 工具函数模块 (`src/app/utils/`)](#44-工具函数模块)
  - [4.5 页面模块 (`src/app/[locale]/`)](#45-页面模块)
  - [4.6 API 路由模块 (`src/app/api/`)](#46-api-路由模块)
  - [4.7 国际化模块 (`src/i18n/` + `messages/`)](#47-国际化模块)
- [5. 翻译服务架构](#5-翻译服务架构)
- [6. 数据流分析](#6-数据流分析)
- [7. 性能优化策略](#7-性能优化策略)
- [8. 部署架构](#8-部署架构)

---

## 1. 项目概述

### 1.1 功能简介

Subtitle Translator 是一个基于 AI 的批量字幕翻译工具，支持 `.srt`、`.ass`、`.vtt` 和 `.lrc` 四种字幕格式，可将字幕文件翻译成 **50+ 种语言**。

### 1.2 核心特性

| 特性 | 描述 |
|------|------|
| **实时翻译** | 采用分块压缩和并行处理，实现每集 1 秒翻译速度 |
| **批量处理** | 支持同时处理数百个字幕文件 |
| **高性能缓存** | 使用 IndexedDB 存储翻译结果，无容量限制 |
| **上下文感知翻译** | AI 模型支持带前后文翻译，提高对话连贯性 |
| **双语输出** | 支持双语字幕，位置可调 |
| **多语言翻译** | 支持单文件同时翻译成多种语言 |
| **字幕提取** | 可提取纯文本用于 AI 摘要等用途 |

### 1.3 支持的翻译服务

**传统机器翻译 API** (6 个):
- GTX Free API (免费)
- Google Translate
- DeepL
- DeepLX (免费)
- Azure Translate
- Qwen-MT

**LLM 模型** (11 个):
- DeepSeek、OpenAI、Claude、Gemini
- Qwen (通义千问)、Moonshot (Kimi)、Zhipu GLM (智谱)
- Doubao (火山方舟)、xAI (Grok)、Mistral、Perplexity

**聚合服务/自托管** (5 个):
- OpenRouter、Groq、SiliconFlow
- Nvidia NIM、Azure OpenAI、Custom LLM

---

## 2. 技术架构

### 2.1 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Next.js | 16.1.6 | React 全栈框架 |
| React | 19.2.4 | UI 库 |
| TypeScript | 5.x | 类型系统 |
| Tailwind CSS | 4 | 样式框架 |
| Ant Design | 6.3.1 | UI 组件库 |
| next-intl | 4.8.3 | 国际化 |
| idb | 8.0.3 | IndexedDB 封装 |
| p-limit | 7.3.0 | 并发控制 |
| p-retry | 7.1.0 | 重试机制 |

### 2.2 架构模式

- **渲染模式**: 静态导出 (Static Export) / Docker 独立部署
- **状态管理**: React Hooks + Context API + localStorage
- **数据持久化**: IndexedDB (翻译缓存) + localStorage (配置)
- **并发控制**: p-limit (限制并发请求数)
- **重试机制**: p-retry (自动重试失败请求)

### 2.3 构建配置

```typescript
// next.config.ts
output: "export"         // 静态站点导出
output: "standalone"     // Docker 模式
reactCompiler: true       // React 编译器优化
optimizePackageImports:  // 包导入优化
```

---

## 3. 目录结构

```
subtitle-translator/
├── src/
│   ├── app/
│   │   ├── [locale]/              # 国际化路由
│   │   │   ├── layout.tsx         # 布局组件
│   │   │   ├── page.tsx           # 服务端入口
│   │   │   ├── client.tsx         # 客户端入口
│   │   │   ├── SubtitleTranslator.tsx    # 主翻译组件
│   │   │   └── subtitleUtils.ts   # 字幕处理工具
│   │   ├── api/                   # API 路由
│   │   │   ├── deepl/             # DeepL 代理
│   │   │   └── nvidia/            # Nvidia 代理
│   │   ├── components/            # React 组件
│   │   ├── hooks/                 # 自定义 Hooks
│   │   │   └── translation/       # 翻译相关 Hooks
│   │   ├── lib/
│   │   │   ├── translation/       # 翻译引擎核心
│   │   │   └── storage/           # 存储模块
│   │   └── utils/                 # 工具函数
│   ├── i18n/                      # 国际化配置
│   └── proxy.ts                   # API 代理
├── messages/                      # 翻译语言文件 (17 种语言)
├── public/                        # 静态资源
├── scripts/                       # 构建脚本
├── package.json
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
└── docker-compose.yml
```

---

## 4. 核心模块详解

### 4.1 翻译引擎模块 (`src/app/lib/translation/`)

#### 4.1.1 模块结构

```
translation/
├── index.ts              # 主入口，导出所有翻译功能
├── types.ts              # TypeScript 类型定义
├── registry.ts           # 翻译服务注册表 (单一数据源)
├── config.ts             # 默认配置
├── cache.ts              # 翻译缓存管理
├── utils.ts              # 翻译工具函数
├── languages-data.ts     # 语言支持数据
└── services/
    ├── index.ts          # 服务分发器
    ├── traditional.ts    # 传统 MT API 实现
    ├── llm.ts            # LLM API 实现
    └── shared.ts         # 共享逻辑
```

#### 4.1.2 核心类型定义

```typescript
// 翻译方法类型 (从 registry 动态派生)
export type TranslationMethod = keyof typeof defaultConfigs | (string & {});

// 翻译参数
export interface TranslateTextParams {
  text: string;                    // 待翻译文本
  targetLanguage: string;          // 目标语言
  sourceLanguage: string;          // 源语言
  translationMethod: string;       // 翻译服务
  cacheSuffix: string;             // 缓存后缀
  useCache?: boolean;              // 是否使用缓存
  apiKey?: string;                 // API 密钥
  url?: string;                    // 自定义端点
  model?: string;                  // LLM 模型
  temperature?: number;            // 温度参数 (0-1.99)
  sysPrompt?: string;              // 系统提示词
  userPrompt?: string;             // 用户提示词
  useRelay?: boolean;              // 是否使用中继
  enableThinking?: boolean;        // 启用思考模式
  domains?: string;                // Qwen-MT 领域设置
  fullText?: string;               // 完整文本变量
  signal?: AbortSignal;            // 取消信号
}

// 翻译配置
export interface TranslationConfig {
  apiKey?: string;
  url?: string;
  region?: string;
  model?: string;
  apiVersion?: string;
  temperature?: number;
  chunkSize?: number;      // 分块大小
  delayTime?: number;      // 延迟时间 (ms)
  batchSize?: number;      // 批处理大小
  contextWindow?: number;  // 上下文窗口
  sysPrompt?: string;
  userPrompt?: string;
  useRelay?: boolean;
  enableThinking?: boolean;
  domains?: string;
}
```

#### 4.1.3 服务注册表 (`registry.ts`)

**单一数据源设计**: 所有翻译服务的配置都在 `PROVIDERS` 对象中定义，其他所有派生数据（UI 列表、默认配置、类型等）都从这里生成。

```typescript
export const PROVIDERS = {
  // 机器翻译
  gtxFreeAPI: { kind: "custom", category: "machine-translation", ... },
  google: { kind: "custom", category: "machine-translation", ... },
  deepl: { kind: "custom", category: "machine-translation", ... },
  azure: { kind: "custom", category: "machine-translation", ... },
  deeplx: { kind: "custom", category: "machine-translation", ... },
  qwenMt: { kind: "custom", category: "machine-translation", ... },

  // LLM 模型
  deepseek: { kind: "openai-compat", category: "llm", ... },
  openai: { kind: "openai-compat", category: "llm", ... },
  claude: { kind: "custom", category: "llm", ... },
  gemini: { kind: "custom", category: "llm", ... },
  // ... 更多 LLM

  // 聚合服务
  openrouter: { kind: "openai-compat", category: "aggregator", ... },
  groq: { kind: "openai-compat", category: "aggregator", ... },
  nvidia: { kind: "custom", category: "aggregator", ... },
} as const;
```

**派生视图**:
- `TRANSLATION_SERVICES`: UI 显示的服务列表
- `defaultConfigs`: 默认配置对象
- `LLM_MODELS`: 所有 LLM 服务数组
- `OPENAI_COMPAT_PROVIDERS`: OpenAI 兼容服务
- `categorizedOptions`: 分类下拉选项

#### 4.1.4 翻译缓存 (`cache.ts`)

基于 IndexedDB 实现的持久化缓存：

```typescript
// 使用 MD5 生成缓存键
export const generateCacheKey = (text: string, cacheSuffix: string): string => {
  const spark = new SparkMD5.ArrayBuffer();
  spark.append(text);
  return `${spark.end()}-${cacheSuffix}`;
};

// 缓存操作
export const getCachedTranslation = async (key: string): Promise<string | null>;
export const setCachedTranslation = async (key: string, value: string): Promise<void>;
export const clearTranslationCache = async (): Promise<number>;
```

#### 4.1.5 OpenAI 兼容服务工厂 (`services/llm.ts`)

统一处理所有 OpenAI 兼容的 LLM API：

```typescript
const createOpenAICompatService = (
  spec: OpenAICompatProviderSpec
): TranslationService => {
  return async (params: TranslateTextParams): Promise<string> => {
    // 1. 构建 Prompt
    // 2. API 调用 (支持中继)
    // 3. 处理思考模式输出
    // 4. 返回结果
  };
};
```

---

### 4.2 React 组件模块 (`src/app/components/`)

#### 4.2.1 组件列表

| 组件 | 功能 | 位置 |
|------|------|------|
| `SubtitleTranslator` | 主翻译界面组件 | `[locale]/SubtitleTranslator.tsx` |
| `TranslationSettings` | API 设置高级面板 | `TranslationSettings.tsx` |
| `TranslationAPISelector` | API 服务选择器 | `TranslationAPISelector.tsx` |
| `LanguageSelector` | 语言选择器 | `LanguageSelector.tsx` |
| `MultiLanguageSettingsModal` | 多语言设置弹窗 | `MultiLanguageSettingsModal.tsx` |
| `TranslationProgressModal` | 翻译进度弹窗 | `TranslationProgressModal.tsx` |
| `ResultCard` | 结果展示卡片 | `ResultCard.tsx` |
| `AdvancedTranslationSettings` | 高级设置面板 | `AdvancedTranslationSettings.tsx` |
| `FaqSection` | FAQ 常见问题 | `FaqSection.tsx` |
| `TranslationContext` | 全局翻译上下文 | `TranslationContext.tsx` |

#### 4.2.2 主翻译组件流程

```
SubtitleTranslator.tsx
├── 文件上传 (useFileUpload)
│   ├── 单文件模式: 粘贴文本
│   └── 批量模式: 拖拽上传
├── 翻译执行 (performTranslation)
│   ├── 字幕格式检测 (detectSubtitleFormat)
│   ├── 内容提取 (filterSubLines)
│   ├── ASS 标签处理 (prepareAssForTranslation)
│   ├── 内容翻译 (translateContent)
│   │   ├── 上下文感知翻译 (LLM)
│   │   └── 分块翻译 (传统 API)
│   ├── 双语字幕生成
│   └── 文件下载
└── 结果展示 (ResultCard)
```

---

### 4.3 自定义 Hooks 模块 (`src/app/hooks/`)

#### 4.3.1 翻译核心 Hook (`useTranslateData.tsx`)

这是整个应用的核心逻辑 Hook，包含：

| 功能 | 说明 |
|------|------|
| `translateContent` | 核心翻译函数，支持三种翻译模式 |
| `translateWithContext` | 上下文感知翻译 (LLM) |
| `retryTranslate` | 带重试和超时的翻译函数 |
| `handleTranslate` | 翻译流程控制器 |
| `validateTranslate` | API 配置验证 |
| `exportSettings/importSettings` | 配置导入导出 |
| `handleLanguageChange` | 语言切换逻辑 |
| `handleConfigChange` | 配置变更逻辑 |

#### 4.3.2 其他 Hooks

| Hook | 功能 |
|------|------|
| `useTranslationProgress` | 翻译进度管理 |
| `useFileUpload` | 文件上传处理 |
| `useLlmPresets` | LLM 预设管理 |
| `useCopyToClipboard` | 剪贴板操作 |
| `useExportFilename` | 文件名生成 |
| `useLocalStorage` | localStorage 封装 |
| `useTextStats` | 文本统计 (字数/行数) |

#### 4.3.3 翻译相关 Hooks (`hooks/translation/`)

| Hook | 功能 |
|------|------|
| `contextTranslation.ts` | 上下文翻译逻辑 |
| `retry.ts` | 重试配置和逻辑 |
| `settings.ts` | 设置管理 |

---

### 4.4 工具函数模块 (`src/app/utils/`)

#### 4.4.1 工具文件列表

| 文件 | 功能 |
|------|------|
| `errorUtils.ts` | 错误处理和消息生成 |
| `fileTypes.ts` | 文件类型配置 |
| `fileUtils.ts` | 文件操作 (读取、编码检测) |
| `localeUtils.ts` | 语言和本地化工具 |
| `localStorageUtils.ts` | localStorage 辅助函数 |
| `regex.ts` | 正则表达式常量 |
| `textUtils.ts` | 文本处理工具 |

#### 4.4.2 字幕处理工具 (`[locale]/subtitleUtils.ts`)

核心字幕处理函数：

| 函数 | 功能 |
|------|------|
| `detectSubtitleFormat()` | 检测字幕格式 (SRT/VTT/ASS/LRC) |
| `filterSubLines()` | 提取字幕内容行 |
| `prepareAssForTranslation()` | 剥离 ASS 标签 |
| `restoreAssAfterTranslation()` | 还原 ASS 标签 |
| `convertTimeToAss()` | 时间格式转换 |
| `getOutputFileExtension()` | 获取输出文件扩展名 |
| `assHeader` | ASS 文件头模板 |

---

### 4.5 页面模块 (`src/app/[locale]/`)

#### 4.5.1 页面层次

```
layout.tsx (根布局)
└── page.tsx (服务端入口)
    └── client.tsx (客户端入口)
        ├── Tab 1: 基础翻译
        │   └── SubtitleTranslator.tsx
        └── Tab 2: 高级设置
            └── TranslationSettings.tsx
```

#### 4.5.2 国际化路由

使用 `next-intl` 实现动态语言路由：

```typescript
// src/i18n/routing.ts
export const routing = defineRouting({
  locales: ["en", "zh", "zh-hant", "pt", "es", "hi", "ar", "fr", "de",
            "ja", "ko", "ru", "vi", "th", "tr", "bn", "id", "it"],
  defaultLocale: "en",
});
```

URL 格式: `/zh/subtitle-translator`

---

### 4.6 API 路由模块 (`src/app/api/`)

#### 4.6.1 代理路由

| 路由 | 用途 |
|------|------|
| `/api/deepl` | DeepL API 代理 (CORS 解决) |
| `/api/nvidia` | Nvidia API 代理 |

代理逻辑使用 `src/proxy.ts` 统一处理。

---

### 4.7 国际化模块 (`src/i18n/` + `messages/`)

#### 4.7.1 国际化配置

| 文件 | 功能 |
|------|------|
| `routing.ts` | 路由配置 |
| `navigation.ts` | 导航封装 |
| `request.ts` | 请求配置 |

#### 4.7.2 语言文件

位于 `messages/` 目录，支持 17 种语言：

```json
// messages/zh.json (示例)
{
  "common": {
    "translate": "翻译",
    "sourceLanguage": "源语言",
    "targetLanguage": "目标语言",
    // ...
  },
  "subtitle": {
    "title": "字幕翻译",
    "clientDescription": "支持 SRT、VTT、ASS、LRC 格式的批量翻译...",
    // ...
  }
}
```

---

## 5. 翻译服务架构

### 5.1 服务分类

```
机器翻译 (6 个)
├── GTX Free API    (免费)
├── Google Translate
├── DeepL
├── DeepLX          (免费)
├── Azure Translate
└── Qwen-MT

LLM 模型 (11 个)
├── DeepSeek        (支持中继)
├── OpenAI          (支持中继)
├── Claude
├── Gemini
├── Qwen            (支持自定义 URL + 中继)
├── Moonshot (Kimi) (支持中继)
├── Zhipu GLM       (支持中继)
├── Doubao          (支持自定义 URL + 中继)
├── xAI (Grok)      (支持中继)
├── Mistral         (支持中继)
└── Perplexity      (支持中继)

聚合服务/自托管 (5 个)
├── OpenRouter      (支持模型选择)
├── Groq            (高速推理)
├── SiliconFlow
├── Nvidia NIM
├── Azure OpenAI
└── Custom LLM      (自部署)
```

### 5.2 OpenAI 兼容服务工厂

统一的处理流程：

```typescript
1. 参数校验
2. 构建请求体
3. 中继模式处理 (可选)
4. 发送 API 请求
5. 思考模式输出处理
6. 错误处理
7. 返回翻译结果
```

### 5.3 思考模式 (Thinking Mode)

支持 kimi、deepseek、glm、gpt-oss 等模型的思考输出解析：

```typescript
// 从输出中提取最终结果
<think>
思考过程...
</think>
最终翻译结果
```

---

## 6. 数据流分析

### 6.1 翻译流程

```
用户操作
└── 上传/粘贴字幕文件
    ↓
文件处理
├── 格式检测 (detectSubtitleFormat)
├── 编码检测 (jschardet)
├── 内容提取 (filterSubLines)
└── ASS 标签处理 (prepareAssForTranslation)
    ↓
翻译请求
├── 参数验证 (validateTranslate)
├── 生成缓存键 (generateCacheKey)
├── 检查缓存
└── 调用翻译服务
    │
    ├→ 传统 API: 分块翻译
    └→ LLM API: 上下文感知翻译
        ├── 构建上下文批次
        ├── 发送批量请求
        ├── 解析标记结果
        └── 逐行还原
    ↓
结果处理
├── ASS 标签还原 (restoreAssAfterTranslation)
├── 双语字幕生成
└── 文件下载
```

### 6.2 上下文感知翻译流程

```
原始字幕行数组
    ↓
按 batchSize 分批
    ↓
为每批次添加上下文 (前后 padding)
    ↓
添加标记
[CONTEXT] 前文
[TRANSLATE_0] 当前行1
[TRANSLATE_1] 当前行2
[TRANSLATE_2] 当前行3
[CONTEXT] 后文
    ↓
发送给 LLM
    ↓
解析标记结果
    ↓
映射回原始位置
    ↓
失败重试 (减小 contextWindow)
```

### 6.3 状态管理流程

```
TranslationContext (全局状态)
├── translationConfigs (所有服务配置)
├── translationMethod (当前服务)
├── sourceLanguage / targetLanguage
├── sysPrompt / userPrompt
├── multiLanguageMode / target_langs
├── translatedText / extractedText
├── translateInProgress / progressPercent
└── llmPresets (LLM 预设)

└── 本地存储同步 (useLocalStorage)
```

---

## 7. 性能优化策略

### 7.1 并发控制

```typescript
// 使用 p-limit 限制并发请求数
const concurrency = Math.max(Number(config?.batchSize) || 10, 1);
const limit = pLimit(concurrency);

// 并发翻译
const promises = contentLines.map((line, index) =>
  limit(async () => {
    translatedLines[index] = await retryTranslate(...);
  })
);
```

### 7.2 重试机制

```typescript
// 使用 p-retry 实现指数退避重试
await pRetry(
  async () => {
    // 翻译逻辑
  },
  {
    retries: retryCount,
    minTimeout: 1000,
    maxTimeout: retryTimeout * 1000,
    onFailedAttempt: ({ error, attemptNumber, retriesLeft }) => {
      console.warn(`Attempt ${attemptNumber} failed...`);
    },
  }
);
```

### 7.3 缓存策略

- **存储**: IndexedDB (无容量限制)
- **键生成**: MD5(text) + cacheSuffix
- **缓存粒度**: 按翻译服务 + 源语言 + 目标语言 + 模型配置

### 7.4 分块翻译

传统 API 使用分块策略减少请求次数：

```typescript
const chunks = splitTextIntoChunks(text, chunkSize, delimiter);
for (const chunk of chunks) {
  const result = await translate(chunk);
  translatedChunks.push(result);
}
```

### 7.5 React 编译器优化

```typescript
// next.config.ts
reactCompiler: true,  // 启用 React 编译器
```

### 7.6 包导入优化

```typescript
experimental: {
  optimizePackageImports: ["antd", "@ant-design/icons"],
}
```

---

## 8. 部署架构

### 8.1 部署模式

| 模式 | 输出 | 说明 |
|------|------|------|
| 静态导出 | `output: "export"` | 适用于 Vercel、Netlify 等平台 |
| Docker | `output: "standalone"` | 适用于自部署，支持 API 路由 |

### 8.2 Docker 部署

```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN yarn install
COPY . .
RUN yarn build
ENV NODE_ENV=production
EXPOSE 3000
CMD ["yarn", "start"]
```

### 8.3 环境变量

无需特殊环境变量，所有配置存储在客户端 localStorage。

---

## 附录

### A. 支持的字幕格式详解

| 格式 | 扩展名 | 特点 |
|------|--------|------|
| **SRT** | .srt | 最常见的字幕格式，简洁易读 |
| **VTT** | .vtt | Web 标准，支持 HTML 标签 |
| **ASS/SSA** | .ass | 功能强大，支持样式、特效 |
| **LRC** | .lrc | 歌词格式，仅时间 + 文本 |

### B. 关键依赖说明

| 依赖 | 用途 |
|------|------|
| `idb` | IndexedDB 的 Promise 封装 |
| `jschardet` | 字符编码检测 |
| `file-saver` | 文件下载 |
| `p-limit` | 并发控制 |
| `p-retry` | 重试机制 |
| `compromise` | 自然语言处理 (NLP) |
| `rtl-detect` | RTL 语言检测 |
| `spark-md5` | MD5 哈希生成 |

### C. 文件类型配置

```typescript
// utils/fileTypes.ts
export const FILE_TYPE_CONFIGS = {
  subtitle: {
    accept: ".srt,.ass,.ssa,.vtt,.lrc",
    label: "SRT, ASS, VTT, LRC",
  },
  markdown: {
    accept: ".md,.markdown",
    label: "Markdown",
  },
  generic: {
    accept: ".txt",
    label: "TXT",
  },
};
```

---

*文档生成时间: 2025年*
*项目版本: 2.2.0*
