# 生产环境 Docker Compose 配置
# 使用示例: docker-compose -f docker-compose.prod.yml up -d

version: "3.8"

services:
  # Next.js 前端服务
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - DOCKER_BUILD=true
    image: subtitle-translator-frontend:latest
    container_name: subtitle-translator-frontend
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
      - DOCKER_BUILD=true
      - NEXT_PUBLIC_USE_LOCAL_API=false
      - NEXT_PUBLIC_SERVER_URL=http://server:3001
    depends_on:
      server:
        condition: service_healthy
    networks:
      - subtitle-network
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # 独立服务端
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    image: subtitle-translator-server:latest
    container_name: subtitle-translator-server
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - HOST=0.0.0.0
      - CORS_ORIGIN=*
      # 缓存配置
      - CACHE_MAX_SIZE=5000
      - CACHE_MAX_AGE=7200000
      # 批量翻译配置
      - MAX_BATCH_SIZE=100
      - DEFAULT_BATCH_SIZE=30
      - MAX_CONCURRENT_REQUESTS=20
      # LLM 中转服务
      - LLM_RELAY_BASE=https://llm-proxy.aishort.top
      # 代理配置（如需要，请取消注释并修改）
      # - HTTP_PROXY=http://proxy.example.com:8080
      # - HTTPS_PROXY=http://proxy.example.com:8080
      # - NO_PROXY=localhost,127.0.0.1
    networks:
      - subtitle-network
    restart: always
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    # 资源限制
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: "2G"
        reservations:
          cpus: "0.5"
          memory: "512M"

networks:
  subtitle-network:
    driver: bridge
    name: subtitle-production-network

volumes:
  # 如需持久化缓存或其他数据，可以添加 volumes
  # cache-data: